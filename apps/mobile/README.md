# EGA Mobile (Expo)

Bootstrap Expo React Native app for EGA House.

## Run locally

```bash
cd apps/mobile
npm install
npm run start
```

Then press:

- `a` for Android
- `i` for iOS (macOS only)
- `w` for web
